{% extends 'base.html.twig' %}

{% block title %}Type{% endblock %}

{% block body %}
<div class="mx-auto w-75 align-items-center article">
    <h1>Type</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ type.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('type_index') }}" class="btn btn-primary mt-2">Retour</a>

    <a href="{{ path('type_edit', {'id': type.id}) }}" class="btn btn-warning mt-2">Modifié</a>

    {{ include('type/_delete_form.html.twig') }}
</div>
{% endblock %}
